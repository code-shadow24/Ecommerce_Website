import { asyncHandler } from "../utils/asyncHandle.js";
import { ApiError } from "../utils/apiError.js";
import { ApiResponse } from "../utils/apiResponse.js";

const generateSalesReport = asyncHandler( async(req, res)=>{

})

const generateProductReport = asyncHandler( async(req, res)=>{

})

const generateCustomerReport = asyncHandler( async(req, res)=>{

})

const generateCategoryReport = asyncHandler( async(req, res)=>{

})

const generateTicketReport = asyncHandler( async(req, res)=>{

})

const generateInventoryReport = asyncHandler( async(req, res)=>{

})

const generateMarketingReport = asyncHandler( async(req, res)=>{

})

const generateCustomReport = asyncHandler( async(req, res)=>{

})

const exportReport = asyncHandler( async(req, res)=>{

})

const scheduleReport = asyncHandler( async(req, res)=>{

})

const viewReport = asyncHandler( async(req, res)=>{

})

export {
    generateSalesReport,
    generateProductReport,
    generateCustomerReport,
    generateCategoryReport,
    generateTicketReport,
    generateInventoryReport,
    generateMarketingReport,
    generateCustomReport,
    exportReport,
    scheduleReport,
    viewReport
}